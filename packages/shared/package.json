{
  "name": "@repo/shared",
  "version": "0.0.0",
  "description": "Shared",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "license": "MIT",
  "dependencies": {
    "decimal.js": "^10.6.0",
    "superjson": "^2.2.2"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*"
  },
  "exports": {
    "./utils/*": "./src/utils/*.ts"
  }
}
